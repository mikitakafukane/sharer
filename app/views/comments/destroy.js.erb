$("#post_<% @post.id %> .comments-count").html("コメント数: <%= @post.comments.count %>");
$("#post_<% @post.id %> .comments_area").html("<%= j(render 'comments/index', post: @post) %>");
$("comment_content").val('')

